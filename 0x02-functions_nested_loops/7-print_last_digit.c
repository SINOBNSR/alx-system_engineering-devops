#include "main.h"
/**
 * print_last_digit - Entry point
 * Description: prints the last digit of a number
 * @n: is an integer
 * Return: integer
 */
int print_last_digit(int n)
{
	int k;

	k = n % 10;
	if (n < 10)
		k = -k;
	return (n);
}
